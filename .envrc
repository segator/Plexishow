#!/usr/bin/env bash

# Auto-install and activate mise tools
if has mise; then
    mise install
    eval "$(mise activate bash)"
elif has asdf; then
    asdf install
else
    echo "⚠  WARNING: Neither mise nor asdf found. Install tools manually."
    echo "   brew install mise"
fi
